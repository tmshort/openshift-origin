apiVersion: olm.operatorframework.io/v1alpha1
kind: ClusterCatalog
metadata:
  name: catalog-test
spec:
  priority: 0
  source:
    type: Image
    image:
      pollInterval: 24h
      ref: example.com/simple-fbc/catalog-test:latest
