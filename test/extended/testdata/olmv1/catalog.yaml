apiVersion: olm.operatorframework.io/v1
kind: ClusterCatalog
metadata:
  name: catalog-test
spec:
  availabilityMode: Available
  priority: 0
  source:
    type: Image
    image:
      pollIntervalMinutes: 600
      ref: {REF}
